# USER.md — your "about you" memory (template)
#
# Copy the lines BELOW the divider into ~/.hermes/memories/USER.md in your VM and fill in
# the <placeholders>. This is injected into the assistant's context EVERY session, so keep
# it tight — Hermes hard-caps it at ~1375 characters and refuses writes that overflow.
# Long-tail detail goes in USER_DETAIL.md (read on demand). Full design: docs/MEMORY.md.
#
# Entries are separated by a single § on its own line. Keep each one compact, high-signal,
# and durable (stable facts — not today's to-dos). Placeholders only; never commit real PII.
# ──────────────────────────────────────────────────────────────────────────────
Name: <your name>. Based in <city>; timezone <IANA tz, e.g. America/Los_Angeles>. <one line: what you do>.
§
Key people: <name> (<relationship>), <name> (<relationship>). Prioritize anything involving them.
§
How I like it: short, casual texts; ask before any email/calendar action; quiet hours <22:00–07:00>.
§
Current focus: <the main goal or project you're on right now>.
§
Full profile in memories/USER_DETAIL.md — read that when a task needs more than this digest.
